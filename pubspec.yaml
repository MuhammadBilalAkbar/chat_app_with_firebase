name: chat_app_with_firebase
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  http: any
  # firebase
  firebase_core: any
  firebase_auth: any
  firebase_storage: any
  cloud_firestore: any
  # Date and Time
  timeago: any
  # media
  image_picker: any
  # state management
  provider: any
  email_validator: ^2.1.17
  firebase_messaging: ^14.1.4
  flutter_local_notifications: ^12.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true